services:
  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: terranthro-server
    restart: unless-stopped
    env_file:
      - ./server/.env.docker
    ports:
      - "8000:8000"
